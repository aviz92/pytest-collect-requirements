[pytest]
log_cli = true
log_cli_level = DEBUG

addopts =
;    --collect-only
    -s
    -v

    --collect-requirements
;    --save-to=logs2/test_requirements.json


markers =
    requirements: mark test requirements
    integration: mark test integration
    integration2: mark test integration2
